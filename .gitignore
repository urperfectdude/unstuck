# Source copy deck. Deliberately untracked: this repo is public and serves via
# GitHub Pages, so anything committed here is publicly downloadable. The deck
# holds internal screening logic and pricing rationale.
04-landing-page.docx

.DS_Store

# Worker secrets and build junk. BOT_TOKEN and CHAT_ID live in Cloudflare's
# secret store, never here.
.dev.vars
node_modules/
.wrangler/
